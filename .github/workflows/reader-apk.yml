name: Build NeuroReader APK

on:
  push:
    branches: [reader-app]
    paths:
      - 'NeuroReader/**'
      - '.github/workflows/reader-apk.yml'
  pull_request:
    branches: [main]
    paths:
      - 'NeuroReader/**'
      - '.github/workflows/reader-apk.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Gradle 8.11.1
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Android SDK 36
        shell: bash
        run: |
          set -euo pipefail
          SDK_ROOT="${ANDROID_HOME:-${ANDROID_SDK_ROOT:-/usr/local/lib/android/sdk}}"
          SDKMANAGER=""
          for candidate in \
            "$SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" \
            "$SDK_ROOT/cmdline-tools/bin/sdkmanager" \
            "/usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager"; do
            if [ -x "$candidate" ]; then SDKMANAGER="$candidate"; break; fi
          done
          if [ -z "$SDKMANAGER" ] && [ -d "$SDK_ROOT" ]; then
            SDKMANAGER="$(find "$SDK_ROOT" -type f -name sdkmanager 2>/dev/null | head -n 1 || true)"
          fi
          if [ -z "$SDKMANAGER" ]; then
            SDK_ROOT="$RUNNER_TEMP/android-sdk"
            mkdir -p "$SDK_ROOT/cmdline-tools"
            curl -L --fail --retry 3 -o "$RUNNER_TEMP/cmdline-tools.zip" https://dl.google.com/android/repository/commandlinetools-linux-15859902_latest.zip
            unzip -q "$RUNNER_TEMP/cmdline-tools.zip" -d "$RUNNER_TEMP/cmdline-unpacked"
            mv "$RUNNER_TEMP/cmdline-unpacked/cmdline-tools" "$SDK_ROOT/cmdline-tools/latest"
            SDKMANAGER="$SDK_ROOT/cmdline-tools/latest/bin/sdkmanager"
          fi
          echo "ANDROID_HOME=$SDK_ROOT" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=$SDK_ROOT" >> "$GITHUB_ENV"
          yes | "$SDKMANAGER" --sdk_root="$SDK_ROOT" --licenses >/dev/null || true
          "$SDKMANAGER" --sdk_root="$SDK_ROOT" 'platform-tools' 'platforms;android-36' 'build-tools;36.0.0'

      - name: Download sherpa-onnx Android AAR
        shell: bash
        run: |
          mkdir -p NeuroReader/app/libs
          curl -L --fail --retry 3 -o NeuroReader/app/libs/sherpa-onnx.aar https://github.com/k2-fsa/sherpa-onnx/releases/download/v1.13.4/sherpa-onnx-1.13.4.aar

      - name: Bundle Russian Supertonic 3 voice pack
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p NeuroReader/app/src/main/assets/tts
          curl -L --fail --retry 3 \
            -o /tmp/supertonic3.tar.bz2 \
            https://github.com/k2-fsa/sherpa-onnx/releases/download/tts-models/sherpa-onnx-supertonic-3-tts-int8-2026-05-11.tar.bz2
          tar -xjf /tmp/supertonic3.tar.bz2 -C NeuroReader/app/src/main/assets/tts
          MODEL=NeuroReader/app/src/main/assets/tts/sherpa-onnx-supertonic-3-tts-int8-2026-05-11
          test -f "$MODEL/duration_predictor.int8.onnx"
          test -f "$MODEL/text_encoder.int8.onnx"
          test -f "$MODEL/vector_estimator.int8.onnx"
          test -f "$MODEL/vocoder.int8.onnx"
          test -f "$MODEL/tts.json"
          test -f "$MODEL/unicode_indexer.bin"
          test -f "$MODEL/voice.bin"

      - name: Build debug APK
        working-directory: NeuroReader
        run: gradle --no-daemon --stacktrace :app:assembleDebug

      - name: Prepare APK
        shell: bash
        run: |
          cp NeuroReader/app/build/outputs/apk/debug/app-debug.apk NeuroReader-v3-Supertonic-arm64.apk
          sha256sum NeuroReader-v3-Supertonic-arm64.apk > NeuroReader-v3-Supertonic-arm64.apk.sha256

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: NeuroReader-v3-APK
          path: |
            NeuroReader-v3-Supertonic-arm64.apk
            NeuroReader-v3-Supertonic-arm64.apk.sha256
          retention-days: 30
